/// <reference path="../typings/index.d.ts" />

import * as assert from 'power-assert';

describe("assert", () => {
  it('has deepEqual', () => {
    const a = {a:0, b:1};
    const b = {a:0, b:1};
  })
});

